<div>
  <h4>Comment by <%= review.author_name %></h4>
  <p class="review"><%= review.body %></p>
  <p>Posted <%= distance_of_time_in_words(review.item.created_at, review.created_at) %> ago</p>
</div>
