<!DOCTYPE html>
<html>
  <head>
    <title>PogHogs</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- Navbar -->
  <nav class="navbar navbar-inverse navbar-static-top">
    <div class="container">
            <!-- NavBar pull left -->
          <div id="navbar" class="navbar-collapse collapse">
             <ul class="nav navbar-nav">
               <li><%= link_to "PogHogs", root_path %></li>
               <li><%= link_to "Shop", items_path %></li>
               <li class="dropdown">
                 <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <% all_categories.each do |category| %>
                      <li><%=link_to category.title, category_items_path(category) %></li>
                    <% end %>
                  </ul>
               </li>
             </ul>

                  <!-- NavBar pull right -->
              <ul style="padding-top: 7px;" class="nav navbar-nav navbar-right">
                <%- unless current_user == nil -%>
                  <li><span class="fa-stack fa-lg"><%= link_to "Log out", logout_path, method: :delete %> </li>
                <%- else -%>
                  <li><span class="fa-stack fa-lg"><%= link_to "Log In", login_path, method: :get %> </li>
                <%- end -%>
                  <li><span class="fa-stack fa-lg"><%= link_to "My Cart", '/cart' %></li>
              </ul>
          </div>
    </div>
  </nav>
  <% flash.each do |type, message| %>
    <%= content_tag :div, message, class: type %>
  <% end %>

    <p>Cart: <%= @cart.total_count %> item(s)</p>
    <%= yield %>
  </body>
</html>
